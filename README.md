# Program to verify protobuf and abseil UB in gentoo

How to compile:

```mkdir build && cmake -DCMAKE_BUILD_TYPE=debug ..```

Ho to run:

```cd build```
```./main```

Please, note: UB present in debug builds
